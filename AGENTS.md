# 项目协作规范

适用于整个仓库。项目处于活跃开发期，不要求兼容旧设计。

## 协作方式：Human in the loop

让用户在实现效果的过程中理解问题、参与选择、判断结果，并共同构建 codebase，是项目的基本协作原则。

- **先建立共同理解。** 开始实现前，用清楚的语言说明当前问题、已知事实、待验证的假设、拟采取的步骤，以及如何判断效果达标。把判断依据讲清楚，让用户能够评估方向。
- **关键选择先讨论。** 对尚未达成共识、会影响效果或后续工作的选择，例如资产与物理表示、模块边界或验收标准，先给出推荐方案、主要替代方案、取舍和依据，等待用户参与决定后再落实。可以先做必要的调查或小型验证来帮助判断。已明确的方向和常规实现细节自行推进，不逐步索要确认。
- **以可观察的小步推进。** 将工作拆成用户能理解和验证的阶段；过程中及时说明正在做什么、为什么这样做、新发现了什么，以及是否需要调整方向。每个阶段给出与目标相关的代码入口、运行命令或可查看的结果，让用户有机会反馈，并将反馈带入后续实现。
- **提供判断所需的证据。** 解释改动如何影响实际行为，区分已验证的事实、推测和未完成项。仿真效果应提供可运行的验证入口及图像、视频或测量结果；单元测试通过不能替代效果验收。暴露失败和不确定性，帮助用户判断问题。
- **文档面向人阅读和运行。** 随实现维护清晰的概念说明、代码结构与入口、设计选择及原因，以及可复现的运行说明。命令写明工作目录、前置环境与资产、必要参数、预期输出和验证方式，补充已知限制及常见失败的排查入口。说明集中在对应文档，避免重复维护；让未参与实现的人也能理解、运行、检查和继续修改项目。

## 实现原则

- **以用户目标组织接口。** 常用流程提供合理默认值和一步完成的操作；内部模块拆分不应变成用户的手动步骤。运行结束直接给出结果和产物路径，让用户方便地查看与判断效果。

- **遵从第一性原理和奥卡姆剃刀。** 从实际需求、事实和必要约束出发，选择足以解决问题的最简单方案，不把简单问题复杂化。
- **优先使用标准工具和直接步骤。** 现有方式足够时，不额外增加脚本、配置、封装或抽象；新增复杂度必须有明确的实际收益。
- **保持单一来源。** 同一份依赖、配置或协议只在一处维护，避免重复声明和多套入口。
- **直接改好并清理旧实现。** 必要时调整接口和模块边界，删除被替代的代码、配置、依赖和文档，不保留仅用于兼容旧设计的适配层或回退分支。
- **完成相关变更。** 同步更新调用和文档，执行与改动相称的检查，保证实现与说明一致。
- **大体积资产不进入 Git。** 下载的机器人／场景资产及转换产物统一存放在已忽略的 `.cache/assets/` 或仓库外的共享资产目录；不提交资产压缩包、网格、纹理等大文件。仓库保留源码、小型配置、资产来源与固定版本信息，以及必要的转换脚本。

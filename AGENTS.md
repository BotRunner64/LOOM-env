# 项目协作规范

本文件适用于整个仓库。

## 活跃开发期原则

本项目目前处于活跃开发期。提出方案和实施修改时，以整体架构清晰、简洁、便于维护为优先目标，不需要为旧设计保留向后兼容。

- **直接采用新的统一设计。** 可以调整接口、配置、数据协议和目录结构；同步更新相关调用、文档和验证，不保留仅用于兼容旧方案的适配层、别名或回退分支。
- **从整体架构判断修改。** 保持职责明确、依赖关系清楚，必要时重构模块边界，避免通过不断追加补丁和条件分支维持不合理的结构。
- **修改时同步清理冗余。** 删除被替代的实现、重复逻辑、无用入口、失效配置、未使用的依赖和过时文档，不以“以后可能有用”为理由保留废弃内容。
- **保持单一权威来源。** 同一份依赖、配置或协议只在一个明确的位置维护；抽象以实际需求和复用为依据，避免过度设计。
- **完成相关变更。** 在当前任务范围内把新设计落地，清理旧引用，并执行与改动相称的检查，保证代码、配置和文档一致。

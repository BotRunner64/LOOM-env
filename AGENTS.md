# 项目协作规范

适用于整个仓库。项目处于活跃开发期，提出修改意见后，不要兼容旧设计，实现新设计后彻底清理旧设计。

## 协作方式：Human in the loop

让用户在实现过程中理解问题、参与选择、判断结果，并共同构建 codebase，是项目的基本协作原则。

- **先建立共同理解。** 开始实现前说明当前问题、已知事实、待验证的假设、拟采取的做法，以及如何判断效果达标。
- **关键选择先讨论。** 尚未达成共识、会影响效果或后续工作的选择，先给出推荐方案、替代方案与取舍，等用户参与决定后再落实；已明确的方向和常规实现细节自行推进。
- **以可观察的小步推进。** 把工作拆成用户能理解和验证的阶段，过程中说明在做什么、为什么这样做、新发现了什么，并给出与目标相关的代码入口或运行入口，让反馈能进入后续实现。
- **提供判断所需的证据。** 说明改动如何影响实际行为，区分已验证的事实、推测和未完成项，暴露失败与不确定性，让用户能自行判断是否达标。
- **文档只写沉淀下来的结论。** 概念说明、代码结构与入口、设计选择及可复现的运行方式，集中在对应文档，避免重复维护。调试过程、一次性验证记录和中间结果不写进文档：这些只对当次实现有意义，留下来会给后续工作带来干扰。

## 轨迹采集运行约定

- **轨迹收集在沙箱外执行。** 运行轨迹采集命令（如 `scripts/collect.py`）时直接使用沙箱外执行；这是用户已明确授权的项目约定，无需重复确认。

## 实现原则

- **以用户目标组织接口。** 常用流程提供合理默认值和一步完成的操作；内部模块拆分不应变成用户的手动步骤。运行结束直接给出结果和产物路径，让用户方便地查看与判断效果。

- **遵从第一性原理和奥卡姆剃刀。** 从实际需求、事实和必要约束出发，选择足以解决问题的最简单方案，不把简单问题复杂化。
- **警惕过度设计引入错误。** 先验证最简单可行方案，不为未经证实的问题预先叠加纠偏、补偿或保护逻辑；新增机制必须针对已观察到的问题，并通过对照验证确认收益，效果变差就删除，不继续叠加机制修补。
- **任务阈值以合理验收为准，不追求越严越好。** 根据任务目标、资产几何和实际可接受的误差范围设置合适的阈值；不要把理想对齐或接近零误差当作默认要求，也不要用缺乏依据的苛刻阈值否决已经完成任务的结果。
- **优先使用标准工具和直接步骤。** 现有方式足够时，不额外增加脚本、配置、封装或抽象；新增复杂度必须有明确的实际收益。
- **物体资产以 USD 自包含。** 所有物体的质量、碰撞和物理材质由 USD 定义；导入、准备及运行均不依赖独立物体 metadata，也不在加载时补值或随机化。缺少必要物理属性的资产列为待处理，通过检查后才接入任务。
- **保持单一来源。** 同一份依赖、配置或协议只在一处维护，避免重复声明和多套入口。
- **直接改好并清理旧实现。** 必要时调整接口和模块边界，删除被替代的代码、配置、依赖和文档，不保留仅用于兼容旧设计的适配层或回退分支。
- **完成相关变更。** 同步更新调用和文档，执行与改动相称的检查，保证实现与说明一致。
- **大体积资产不进入 Git。** 下载的机器人／场景资产及转换产物统一存放在已忽略的 `.cache/assets/` 或仓库外的共享资产目录；不提交资产压缩包、网格、纹理等大文件。仓库保留源码、小型配置、资产来源与固定版本信息，以及必要的转换脚本。
